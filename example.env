# --- Database configuration ---
DB_USER="postgres"
DB_PASS="change_me_123!"
DB_PORT="5432"
DB_NAME="devtul_api"
DB_HOST="localhost"

# --- Controller API configuration ---
CONTROLLER_API_HOST="localhost"
CONTROLLER_API_PORT="8111"
CONTROLLER_API_LOG_LEVEL="debug"

# --- Converter API configuration ---
CONVERTER_API_HOST="localhost"
CONVERTER_API_PORT="8112"
CONVERTER_API_LOG_LEVEL="debug"

# --- UI Server Configuration ---
UI_SERVER_HOST="localhost"
UI_SERVER_PORT="8115"
UI_SERVER_LOG_LEVEL="debug"

# --- API Authentication ---
SECRET_KEY="s00p3r_s3cr3t_k3y"
ADMIN_USERNAME="api_admin"
ADMIN_PASSWORD="api_admin"

# --- MQTT Configuration ---
# Used for asynchronous tasks like TTS generation
MQTT_BROKER="localhost"
MQTT_PORT=1883
MQTT_USERNAME="user"
MQTT_PASSWORD="password"
MQTT_TOPIC_PREFIX="controller_api"

# --- S3 / Object Storage Configuration ---
# Used for storing large files (PDFs, HTML, Audio)
# For local MinIO, use http://localhost:9000 (or the container name if running in docker)
S3_ENDPOINT="http://localhost:9000"
S3_ACCESS_KEY="minioadmin"
S3_SECRET_KEY="minioadminpassword"
S3_BUCKET="utilsrvr-data"
# Presigned URL expiration time in seconds (1 hour)
S3_PRESIGNED_URL_EXPIRATION="3600"

# --- TTS Configuration ---
# Directory where Piper ONNX models are stored
PIPER_MODELS_DIR="/models/piper"
# Default voice model name (filename without extension)
TTS_MODEL="en_US-libritts-high"
# Default speaker ID (0 is usually correct for single-speaker models)
TTS_SPEAKER_ID="0"

# --- System Paths ---
# Optional: Override the root data directory for the application
# UTILSRVR_DATA_ROOT="/app/data"

OLLAMA_HOST="http://localhost:11434"
OLLAMA_MODEL="gpt-oss:20b"
OLLAMA_CTX=65536

# --- Server Timezone configuration ---
SERVER_TIMEZONE_OFFSET_HOURS="-5"

# ---- OPTIONAL ---> Gotify Notification Configuration ---
GOTIFDY_API_URL = "https://gotify.example.com"
GOTIFY_API_TOKEN = "your_gotify_api_token"
GOTIFY_SERVICE_NAME = "ControllerAPI"


# --- OPTIONAL ---> Praw Reddit Configuration for Reddit Data Fetching ---
REDDIT_CLIENT_ID="your_reddit_client_id"
REDDIT_CLIENT_SECRET="your_reddit_client_secret"
REDDIT_USER_AGENT="your_app_user_agent"
